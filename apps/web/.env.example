# Sakspilot Web — eksempel .env.local

# API-URL (peker mot @sakspilot/api). Brukes til:
# 1. Klientsidens fetch-kall via Next.js rewrite (proxy)
# 2. Server-side rendering (kommer senere)
NEXT_PUBLIC_API_URL=http://localhost:8001
